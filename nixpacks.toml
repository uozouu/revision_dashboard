[phases.setup]
nixPkgs = ["python311", "file"]

[phases.install]
cmds = ["cd Backend && pip install -r requirements.txt"]

[start]
cmd = "cd Backend && gunicorn -k gevent -w 1 app:app --bind 0.0.0.0:$PORT"

[variables]
PYTHONUNBUFFERED = "1"
